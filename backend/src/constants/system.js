/**
 * System-wide constants and configurations
 */
const SYSTEM_CONSTANTS = {
    // Threshold to define a "New Employee" based on their join date (in days)
    NEW_EMPLOYEE_THRESHOLD_DAYS: 90,
    
    // Other system defaults
    DEFAULT_PAGINATION_LIMIT: 10,
    MAX_PAGINATION_LIMIT: 100
};

module.exports = SYSTEM_CONSTANTS;
